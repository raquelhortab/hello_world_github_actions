<%= form_for(@favourite_food) do |f| %>
  <% if @favourite_food.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@favourite_food.errors.count, "error") %> prohibited this favourite_food from being saved:</h2>

      <ul>
      <% @favourite_food.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :dog %><br />
    <%= f.text_field :dog %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
