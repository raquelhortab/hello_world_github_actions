name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: raquelhortab/hello_world_github_actions/checkout@v1
    - uses: raquelhortab/hello_world_github_actions/.github/actions/automatic-tests@v1
      name: Test
