name: CI

on:
  push:
    branches:
      - master

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout
      name: Checkout
    - uses: ./.github/actions/automatic-tests@master
      name: Test
